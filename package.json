{
  "name": "massage-subscription-monorepo",
  "version": "0.1.0",
  "private": true,
  "description": "Fullstack monorepo for a massage chain subscription platform.",
  "packageManager": "npm@10.9.2",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspace @massage/shared && npm run build --workspace server && npm run build --workspace client",
    "build:shared": "npm run build --workspace @massage/shared",
    "dev": "npm run build --workspace @massage/shared && concurrently \"npm run dev --workspace server\" \"npm run dev --workspace client\"",
    "seed": "npm run seed --workspace server",
    "docker:up": "docker compose up -d",
    "docker:down": "docker compose down",
    "typecheck": "npm run build --workspace @massage/shared && npm run build --workspace server && npm run typecheck --workspace client"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
